% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{conf.est.hard.fast}
\alias{conf.est.hard.fast}
\title{Fast hard-mode sandwich inference}
\usage{
conf.est.hard.fast(
  par,
  X,
  y,
  qhat,
  qhat.se,
  Hmat,
  A,
  lambda.diag = 0,
  hess_precomputed = NULL
)
}
\arguments{
\item{par}{Hard-format parameter vector (beta, Theta, alpha, tmat).}

\item{X}{Design matrix (n x p).}

\item{y}{Integer response vector of length n (class labels 1..K).}

\item{qhat}{External probability matrix (n x (L-1)).}

\item{qhat.se}{Standard errors of external probabilities (n x (L-1)).}

\item{Hmat}{Basis matrix for EL constraints.}

\item{A}{Group matrix (K x L) from \code{group_matrix()}.}

\item{lambda.diag}{Ridge stabilization added to the Hessian diagonal. Default 0.}

\item{hess_precomputed}{Optional precomputed Hessian matrix. If NULL (default),
the Hessian is computed internally via \code{obj.hessian.hard}.}
}
\value{
A list with the same structure as \code{\link{conf.est.hard}}.
}
\description{
Optimized version of \code{\link{conf.est.hard}} that uses the fast
direct Hessian computation (\code{obj.hessian.hard}) and
vectorized qhat-uncertainty adjustment (\code{compute_adj_term}).
Supports optional precomputed Hessian to avoid redundant computation.
}
