% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{ml_fused}
\alias{ml_fused}
\title{Fused Multinomial Optimizer}
\usage{
ml_fused(
  par,
  X,
  y,
  qhat,
  Hmat,
  groups,
  maxit = 500,
  tol = 1e-06,
  learning.rate = 0.1,
  batch_frac = 1,
  lambda = NULL,
  lambda.diag = 0,
  compute_se = TRUE,
  store_trace = FALSE,
  diagnostic_trace = FALSE,
  tau_tmat = 1
)
}
\arguments{
\item{par}{Initial parameters in packed format (via \code{pack_hard()}).}

\item{X}{n x p covariate matrix.}

\item{y}{Integer class labels (1..K).}

\item{qhat}{n x (L-1) external predicted group probabilities.}

\item{Hmat}{n x ((L-1)*H) basis matrix.}

\item{groups}{List of length L defining class-to-group mapping.}

\item{maxit}{Maximum Newton iterations (default 500).}

\item{tol}{Convergence tolerance on gradient infinity-norm (default 1e-6).}

\item{learning.rate}{Step size for Newton update (default 0.1).}

\item{batch_frac}{Mini-batch fraction in (0,1] (default 1).}

\item{lambda}{EL constraint weight. The EL term is scaled by
\code{lambda / n}. Default \code{NULL} uses \code{lambda = n}
(ratio = 1). Set to the external sample size to upweight the
EL constraint.}

\item{lambda.diag}{Ridge stabilization for Hessian diagonal (default 0).}

\item{compute_se}{Compute standard errors at convergence (default TRUE).}

\item{store_trace}{Store parameter trace (default FALSE).}

\item{diagnostic_trace}{Per-iteration diagnostics (default FALSE).}

\item{tau_tmat}{L2 penalty on tmat parameters (default 1).}
}
\value{
A list with:
\describe{
\item{par}{Unpacked best parameters.}
\item{conv}{0 = converged, 1 = max iterations.}
\item{best_grad_norm, best_iter}{Convergence diagnostics.}
\item{grad.error, obj.record}{Per-iteration traces.}
\item{conf}{Full sandwich SE output (from \code{sandwich_se}).}
\item{se}{Sandwich SEs (unpacked list: beta, Theta, alpha, tmat).}
\item{diag_trace}{Diagnostic trace (if requested).}
}
}
\description{
Fits a multinomial logistic regression model on primary data while
incorporating external ML predictions via an empirical-likelihood
constraint. Uses Newton updates with analytic gradient and Hessian.
}
\section{Reference conventions}{

\itemize{
\item Class 1 is the baseline in the softmax parameterization.
\item \code{groups[[1]]} is the reference group.
}
}

