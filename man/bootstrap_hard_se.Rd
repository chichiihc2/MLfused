% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{bootstrap_hard_se}
\alias{bootstrap_hard_se}
\title{Bootstrap Standard Errors for Hard-Mode Fused Multinomial Model}
\usage{
bootstrap_hard_se(
  par_fit,
  X,
  y,
  qhat,
  qhat.se,
  Hmat,
  groups,
  B = 200,
  maxit = 100,
  tol = 1e-04,
  learning.rate = 0.1,
  lambda = NULL,
  lambda.diag = 0
)
}
\arguments{
\item{par_fit}{A fitted parameter list as returned by \code{ML_fused()$par},
containing elements \code{beta}, \code{Theta}, \code{alpha}, and \code{tmat}.}

\item{X}{Numeric matrix of covariates (n x p).}

\item{y}{Integer vector of class labels (length n).}

\item{qhat}{Numeric matrix of external predicted probabilities for non-reference
groups (n x (L-1)).}

\item{qhat.se}{Numeric matrix of standard errors for \code{qhat} (n x (L-1)).}

\item{Hmat}{Numeric basis matrix for the EL constraint (n x ((L-1)*H)).}

\item{groups}{Integer vector of group assignments (length n).}

\item{B}{Integer number of bootstrap replicates (default 200).}

\item{maxit}{Integer maximum iterations per bootstrap fit (default 100).}

\item{tol}{Numeric convergence tolerance (default 1e-4).}

\item{learning.rate}{Numeric step-size scaling factor (default 0.1).}

\item{lambda}{Numeric regularization parameter for the q-penalty. If \code{NULL},
defaults to \code{n}.}

\item{lambda.diag}{Numeric ridge stabilization added to the Hessian diagonal
(default 0).}
}
\value{
A numeric vector of bootstrap standard errors with the same length and
attributes as the packed hard parameter vector.
}
\description{
Computes bootstrap standard errors for the hard-mode (fixed-q)
empirical-likelihood fused multinomial model by resampling observations
and refitting via \code{\link{ML_fused}}.
}
