% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{.build_hard_result_orig}
\alias{.build_hard_result_orig}
\title{Build hard-mode result list (original)}
\usage{
.build_hard_result_orig(
  par_best,
  X,
  y,
  qhat,
  qhat.se,
  Hmat,
  A,
  lambda.diag,
  compute_se,
  best_grad_norm,
  best_iter,
  grad.error,
  obj.record,
  conv,
  par_trace
)
}
\arguments{
\item{par_best}{Numeric vector of best parameters found during
optimization, in hard-packed format (see \code{pack.hard}).}

\item{X}{Numeric matrix of primary covariates (\eqn{n \times p}).}

\item{y}{Integer vector of class labels of length \eqn{n}.}

\item{qhat}{Numeric matrix of external grouped-probability predictions
(\eqn{n \times (L-1)}).}

\item{qhat.se}{Numeric matrix of standard errors for \code{qhat}
(\eqn{n \times (L-1)}).}

\item{Hmat}{Numeric matrix of constraint basis functions
(\eqn{n \times (L-1)H}).}

\item{A}{Group matrix (\eqn{K \times L}) from \code{group_matrix}.}

\item{lambda.diag}{Numeric ridge stabilization for the Hessian.}

\item{compute_se}{Logical; if \code{TRUE}, compute standard errors.}

\item{best_grad_norm}{Numeric; smallest gradient infinity-norm found.}

\item{best_iter}{Integer; iteration at which \code{best_grad_norm} was
attained.}

\item{grad.error}{Numeric vector of gradient norms across iterations.}

\item{obj.record}{Numeric vector of objective values across iterations.}

\item{conv}{Integer convergence flag: 0 = converged, 1 = max iterations.}

\item{par_trace}{List of parameter vectors across iterations, or
\code{NULL} if tracing was disabled.}
}
\value{
A list with components:
\describe{
\item{par}{Unpacked best parameters (from \code{unpack.hard}).}
\item{best_grad_norm}{Smallest gradient norm attained.}
\item{best_iter}{Iteration of best gradient norm.}
\item{grad.error}{Per-iteration gradient norms.}
\item{obj.record}{Per-iteration objective values.}
\item{conv}{Convergence flag (0 or 1).}
\item{conf}{Full output of \code{conf.est.hard}, or \code{NULL}.}
\item{thm3}{Full output of \code{conf.est.thm3}, or \code{NULL}.}
\item{se, se.adj}{Hessian-based SEs (unpacked), with/without qhat
uncertainty adjustment.}
\item{se.score, se.score.adj}{Score-based sandwich SEs (unpacked).}
\item{se.thm3, se.thm3.adj}{Theorem 3 SEs (unpacked).}
\item{se.thm3.score, se.thm3.score.adj}{Theorem 3 plugin SEs
(unpacked).}
\item{par_trace}{Parameter trace list, or \code{NULL}.}
}
}
\description{
Internal helper that constructs the return list for the
hard-mode (fixed-q) branch of \code{\link{ML_fused}}.
Computes sandwich and Theorem 3 standard errors when
\code{compute_se = TRUE}; otherwise returns \code{NULL} SE slots.
}
\details{
Build Result List for Hard-Mode Optimizer (Original)
}
\keyword{internal}
